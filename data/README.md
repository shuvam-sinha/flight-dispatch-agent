# Data

CSVs in this directory are downloaded from [OurAirports](https://ourairports.com/data/)
via `scripts/download_data.py` and are not committed to version control (see
`.gitignore`). Run the download script after cloning:

```bash
python scripts/download_data.py
```
